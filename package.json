{
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "product": "concurrently \"cd client && npm run dev\" \"cd server && npm start\"",
    "setup": "npm install && concurrently \"cd client && npm install\" \"cd server && npm install\"",
    "client": "cd client && npm run dev",
    "server": "cd server && npm start"
  }
}